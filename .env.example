# === Project (Compose stack names; keep bridge and persistence different so starting persistence does not stop the bridge) ===
SMS2MQTT_BRIDGE=sms2mqtt
SMS2MQTT_PERSISTENCE=sms2mqtt-persistence

# === Main bridge (sms2mqtt) ===
# Modem device (mount host device in compose or pass at run)
DEVICE=/dev/mobile
# PIN=
PREFIX=sms2mqtt
HOST=localhost
PORT=1883
CLIENTID=sms2mqtt
# USER=
# PASSWORD=
USETLS=false
LOG_LEVEL=INFO
# MOREINFO=
# HEARTBEAT=
# GAMMUOPTION=
# SMS_MAX_TEXT_LENGTH=
# DEVMODE=0

# === Production image (for compose.production.yml) ===
# DOCKER_REGISTRY=ghcr.io
# DOCKER_IMAGE=your-org/sms2mqtt
# VERSION=latest

# === Optional persistence (docker-compose.persistence.yml) ===
# PGPASSWORD=sms2mqtt
# POSTGRES_IMAGE=postgres:17-alpine   # Override if Docker Hub pull times out (e.g. postgres:16-alpine or mirror)
# MQTT_HOST=localhost
# MQTT_PORT=1883
# MQTT_PREFIX=sms2mqtt
# MQTT_USER=
# MQTT_PASSWORD=
# MQTT_USETLS=false
